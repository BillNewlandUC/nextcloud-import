# Shared content destined for a Nextcloud Team (group) folder.
#
#   <rclone-remote>:<path> | <group folder name> | <nextcloud group>
#
# The source is whichever person's OneDrive currently holds it. The
# group folder is created if missing and granted to the group.
# Comment every line out if you have nothing shared.

od-bill:Shared | Family | family
